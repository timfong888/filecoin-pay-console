import { BigInt as GraphBN } from "@graphprotocol/graph-ts";

export const ONE_BIG_INT = GraphBN.fromU32(1);

export const DEFAULT_DECIMALS = GraphBN.fromU32(18);

export const COMMISSION_MAX_BPS = GraphBN.fromU32(10000);

export const EPOCH_DURATION = GraphBN.fromU32(30);
